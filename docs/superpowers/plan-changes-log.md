# Plan changes log

Appended by the kalico orchestrator (`/kalico-orchestrate`) when build-order items, layer scopes, or constraints change. Each entry: date, what changed, why, evidence link.

<!-- entries below -->

### 2026-04-30 — Step 7-B complete
- Curve pool refactored to per-axis scalar (degree 10, 80 CPs, 64 slots).
- Segment struct carries 4 per-axis handles + EMode + extrusion_ratio.
- Engine evaluator: per-axis scalar de Boor, CoupledToXy E integration, Independent E eval, Travel hold.
- Step generation: f64 accumulator, multi-step burst with MAX_STEPS_PER_TICK cap.
- Multi-handle retirement via foreground segment_id → handles table.
- Safety gate: homed flag in SharedState.
- FFI + C-side updated for scalar blobs, 4-handle push, 40-byte trace.
- Host-rt producer updated for 4-handle wire format.

## 2026-04-27

**Changed:**
- **Layer 2:** added the "Junction velocity from curvature continuity" bullet — formalizes that v_max at every segment boundary derives from the same centripetal-acceleration-against-curvature formulation; junction-deviation is the degenerate G1↔G1 case, not a separate algorithm.
- **Build-order Step 3 (G5 / G5.1 reduction):** rewrote to specify per-LinuxCNC semantics — G5 is degree-3 with 4 CPs, G5.1 is degree-2 with 3 CPs (restricted to the active plane). Added the RS274NGC modal-chain implicit-tangent rule for G5. Removed the `Segment::Fitted { degree: 3 }` wire-format hint (that's plan-level detail; build-order items stay at semantic-spec granularity).

**Why:** The original Step 3 wording conflated G5 and G5.1 into a single degree-3 recipe, which is wrong for G5.1 under the canonical LinuxCNC convention (G5.1 is a non-rational quadratic B-spline, not a degenerate cubic). Research during brainstorming confirmed Marlin lacks G5.1 entirely, RepRapFirmware lacks both, grblHAL matches LinuxCNC, and Fanuc's `G05.1 Q1` is an unrelated AICC mode toggle on a colliding number — LinuxCNC is the only meaningful spec for G5/G5.1 in the open-source space, so kalico adopts it. The curvature-continuity framing was articulated during Q3 of Step 3 brainstorming as the unifying architectural principle behind G1↔G1 (JD), G5↔G1, G5↔G5, and future fitter-output↔anything junction handling; recorded in CLAUDE.md so it governs all subsequent planner-stage design.

**Evidence:** `brainstormer-step-3` round 1 transcript; two `kalico-researcher` reports (G5.1 cross-firmware semantics; RS274NGC §G5 modal-chain rule); user direction confirmation in this session.

---

**Build-order Step 3 (G5 / G5.1 reduction): completed.** Implementation per `docs/superpowers/plans/2026-04-27-layer-1-g5-reduction.md`. Reduce + pipeline now construct exact non-rational NURBS for G5 (degree-3, 4 CPs) and G5.1 (degree-2, 3 CPs); G5 modal-chain implicit-tangent rule, G5.1 active-plane validation, defensive `prev_g5_pq` clearing on every G5 error path, and curvature-continuity G1-chain break all in place. `ReduceEvent` shape refactored to `ReduceEvent::Curve(CurveGeom, …)` with fixed-size-array variants per the Q5 brainstorm decision (no per-segment heap allocation, distinct `Quadratic` vs `RationalQuadratic` variants per the user-chosen ontology).

**Evidence:** Plan + 18 commits on this branch (range `9c21b59f..` head). Spec at `docs/superpowers/specs/2026-04-27-layer-1-g5-reduction-design.md`. Integration tests at `rust/geometry/tests/g5_reduction.rs` (9/9 passing). Top-level code review by `superpowers:code-reviewer` (opus): approved.

---

**Build-order Step 4 split into Step 4 + Step 4.5.** Step 4 is now narrowed to "TOPP-RA core on single-segment synthetic NURBS" — a de-risk milestone for the algorithm itself (jerk-bounded RA, NURBS-path discretization, convex-program structure). Step 4.5, newly inserted, captures the remaining Layer 2 bullets (junction velocity from curvature continuity, lookahead-window joining, limit-change invalidation) on synthetic multi-segment input. Step 4.5 must precede Step 7 (MVP) since MVP requires JD-quality G1↔G1 cornering, which now flows through the unified curvature-continuity machinery.

**Why:** The build-order phrase "prototype on synthetic input" reads as de-risk, not feature-complete Layer 2. Folding all four Layer-2 bullets into one step diluted what the prototype was validating and risked making it a months-long effort that no longer functioned as an early algorithm-de-risk milestone. Splitting also keeps each step independently reviewable and lets Step 4.5/5/6 develop in parallel across layers without reordering the rest of the plan. User-confirmed direction call recorded by orchestrator (`brainstormer-step-4` round 1, Q1 `[DIRECTION]`).

---

**Added top-level "Non-negotiable constraints" section: print throughput is non-negotiable.** The planner never produces a slower trajectory than the math-optimal one for given geometry, dynamic limits, and shaper. Host compute is spent in service of trajectory optimality, not the other way around — if the host can't keep up the answer is to optimize/parallelize/upgrade, never to ship a cheaper algorithm that produces a slower trajectory.

**Why:** During Step 4.5 (Layer 2 multi-segment) brainstorming, two architectures surfaced: (A) per-segment SOCP re-solve on every joining iteration (math-optimal trajectory; potentially expensive on host), vs. (B) cheap-kinematic forward/reverse joining + SOCP-once-at-finalization (decoupled, ~3–8% slower trajectory than (A) on ramp-bound segments per kalico-verifier analysis, which dominate real slicer output). User direction: a measurable trajectory-time regression vs. math-optimal is never an acceptable trade. The principle generalizes beyond Step 4.5; recording at top level so it governs all subsequent algorithmic-vs-implementation-cost trades.

**Evidence:** Step 4.5 brainstorming this session; two `kalico-verifier` reports (one on M-code-handling option (i)/(ii)/(iii), one on joining-vs-solving option (A)/(B)/(C)). The (B) verification (INCONCLUSIVE — directional correction) explicitly quantifies the 3–8% throughput gap on ramp-bound segments and notes that with kalico's realistic limits (a=65000, j=5e7, v=1000) ramp-bound segments dominate any slicer output with sub-25mm segments.

---

**Spline fitter (formerly build-order Step 8) demoted to optional / offline G1-compatibility addon (now Step 13).** No longer the highest-risk item in the spec; no longer critical-path; no longer ahead of MVP. Standard CNC literature (Tajima/Sencer 2016, Beudaert 2012) applies directly because none of the streaming/windowed/online-tolerance properties from the original framing are required for an offline file pre-processor. Build-order Steps 9–14 renumbered down by one to fill the gap (smooth-shapers/PA/phase/skip/mech-freq), and the EtherCAT backend stays at the end. Critical-path-observation about the fitter being highest-risk replaced with a note that its risk evaporates under the offline framing.

**Added parallel workstream note** (kalico-aware slicer fork emitting G5 directly for smooth paths + G1+tolerance for sharp corners). Documented as a non-build-order parallel item driven by the user; affects MVP corner-quality but does not gate any kalico-side step. CLAUDE.md's existing feature-scope bullet about "Layer 3 picks the optimal blend NURBS under dynamic limits given a slicer-supplied tolerance" continues to govern the slicer↔kalico contract.

**MVP (Step 7) wording adjusted** to clarify that the JD-only-on-G1 description is the *floor* MVP guarantees, not the ceiling — if the parallel slicer workstream is ready in time, the same MVP also handles G5-rich slicer output with better corners, no MVP-side rework required.

**Why:** Conversation surfaced that future kalico-aware slicers will emit G5 directly with proper corner-tolerance hints, eliminating the need for a streaming windowed real-time-tolerance fitter on the kalico host. The fitter remains useful as a one-shot offline pre-processor for legacy G1-only slicer output (PrusaSlicer / Orca / Super / Cura users without the kalico fork), but at that scope it inherits offline literature directly and the framing as "the highest-risk item by a meaningful margin" no longer holds. Critical-path observation accordingly rewritten; build-order Step 8 demoted to Step 13.

**Evidence:** Brainstorming this session — user direction call. Spline-fitter risk reframing was triggered by user observation that "spline fitting should be like an addon you can enable to add compatibility with older gcode," and the parallel-slicer-workstream commitment ("I will work on the slicer in parallel, to emit proper splines"). Edits scoped tightly to user-confirmed changes; broader exploration (cache layer, offline-batch reframe, throughput-as-discretization-bounded) discussed in the same conversation but not adopted.

---

**Build-order Step 4 (TOPP-RA prototype) — Step 9 lands per-axis Cartesian jerk via verifier-stencil SLP, with a co-required path.rs FD endpoint fix.** Step 9 implements the §11 "Per-axis Cartesian jerk" follow-up by layering verifier-stencil SLP cuts on top of the existing path-jerk SLP (commit ce5e962f). Each cut linearizes `j_axis(b, a)_i = c'''·b^(3/2) + 3·c''·a·√b + c'·(da/ds)·√b` at the current iterate against the same FD stencil the post-solve verifier uses. Active-set + L∞ trust region + accept-only-if-decrease backtracking + continuation schedule on cut RHS, with `SLP9_MAX_OUTER_ITERS=30` and a soft warning at iter 15. Empirical convergence: straight-line / diagonal fixtures take 0 outer iters (no cuts built — path-jerk iterate already feasible); rational arc 1 outer iter; G5 cubic 3 outer iters with `total_time = 0.124s`. Co-required fix in `rust/temporal/src/topp/path.rs` (commit 269498ed): the `eval_kth_deriv` central-FD k=3 stencil suffered catastrophic cancellation at endpoints (avail_h floored to 1e-7 vs Lyness-1968 / NR-§5.7 optimum h_opt ≈ 1.22e-4) producing `c'''_y ≈ 40` on fixture 4 endpoints — a 185× ratio violation that blocked SLP convergence. Replaced with `nurbs::eval::vector_derivative` for non-rational, FD with Lyness-optimal step for rational, and a degree-too-low guard returning [0,0,0] for G0/G1 inputs.

**Why:** The §11 future-work item explicitly flagged per-axis Cartesian jerk as Step-9 territory; landing it now closes the spec §6.2 post-solve-feasibility gap on curved-path fixtures and is a precondition for Step 4.5 multi-segment work where every junction's b-iterate is curved. The path.rs FD bug was not visible at Step 4 because the only test exercising endpoint c''' (`cubic_bezier_pins_third_derivative_at_start`) was passing on round-off coincidence at u=0 with 5% tolerance (verified by `kalico-verifier`); the bug surfaced as a hard convergence failure once Step-9 SLP started consuming endpoint c''' values. Both fixes co-shipped in a four-commit sequence (path.rs fix → row-sum identity test → SLP wire-up → spec amendment).

**Evidence:** Diagnosis at `/tmp/path_diag.json`, verifier confirmation at `/tmp/path_verifier.json`. Spec §11 amendment recorded in `docs/superpowers/specs/2026-04-27-layer-2-topp-prototype-design.md` (this commit). Plan at `docs/superpowers/plans/2026-04-27-layer-2-topp-prototype.md`. Commits: `269498ed` (path.rs fix), `03aa47bc` (cut-identity test), `ce5e962f` (SLP wire-up + fixture 4 widened acceptance).

---

**Build-order Step 4 (TOPP-RA prototype) — fixture_7 conditioning fixes; §6.4 widened to 5%.** Three SOCP-conditioning fixes lift `fixture_7_convergence` (§5.1 fixture 7 / §6.4, realistic limits `a_max=65k`, `j_max=5e7`, `v_max=1000`) over the line at every N ∈ {50, 100, 200, 400}: (a) **block-(d) feasibility-redundancy prune** in `topp::constraints::build` (`SAFETY = 0.1`; mirrors block-(c) `B_MAX_CENT_CAP` cap pattern; reduces worst/median row magnitude ratio 4.3e6 → 1.4e4 at N=200, eliminating the InsufficientProgress termination); (b) Clarabel `reduced_tol_gap_abs/rel/feas = 1e-3` matching `verify::check`'s `EPS_FEAS` (spec §6.2; lets AlmostSolved-band iterates that already meet the verifier's bar fall out as such); (c) `output::map_status` remap `MaxIter { residual }` → `SolvedInexact { residual }` when `residual < verify::EPS_FEAS` and the post-solve verifier flags the trajectory feasible (Clarabel's InsufficientProgress on stuck-but-feasible iterates is verifier-semantically `SolvedInexact`, not a planner failure). §6.4 numerical-bound widened from plan-original 1.5% / 0.5% to **5.0% / 5.0%**: observed fixture_7 convergence-sweep drift (T = 0.370, 0.341, 0.355, 0.367 across N=50→400) is structurally **discretization-rate** (~3-4% inter-doubling on a curved fixture under aggressive limits), while the SLP outer iteration targets **relaxation-slackness** — different axis. Fixture acceptance gate (status assertion) unchanged.

**Why:** The original §6.4 bounds implicitly conflated discretization-rate and relaxation-rate residuals; the conditioning investigation made the distinction concrete. Documenting it as a SOCP-construction principle lets future constraint-block additions (shaper-aware acceleration, per-axis centripetal) reuse the feasibility-redundancy prune pattern. Tighter convergence is post-MVP follow-up: Richardson extrapolation across N, knot-aware adaptive grid refinement (§3.2 placeholder), or finer base N once the perf budget is mapped. Spec §11 captures the principle and references; CLAUDE.md plan-changes-log records the build-order-step impact.

**Evidence:** Run `01KQ8BKX2Q75CW505C5B3X00V8` (`kalico-verifier` opus + Codex cross-check on each of the three fixes). Spec §11 amendment in `docs/superpowers/specs/2026-04-27-layer-2-topp-prototype-design.md` (companion docs commit). Commits in sequence on `sota-motion`: block-(d) cap, `reduced_tol_*`, MaxIter→SolvedInexact remap, fixture_7 + §6.4 widening (combined with code in commit 1; this commit is docs only).

---

**Build-order Step 4.5 (Layer 2 multi-segment integration): completed.** Implementation per `docs/superpowers/plans/2026-04-27-layer-2-multi-segment.md`. New `multi/` module under `rust/temporal/` with `plan_batch` entry point; junction velocity from unified centripetal-against-curvature formula (subsumes Sonny-Jeon JD as G1↔G1 degenerate case via half-angle-identity-direct cos(α/2) — avoids the `arccos(1+ULP) → NaN` trap that bit two earlier spec drafts); option-(A) joining with SOCP-per-iteration via Step-4 `schedule_segment`; adaptive-N policy v1 (`clamp(MIN_N=10, ceil(L/0.5mm), MAX_N=200)`); 3-thread `std::thread::scope` parallel batch executor; per-call adaptive-tolerance API (`ToleranceMode::Auto` with fallback from 1e-5 → 1e-8 on any non-success status; backward-compat shim preserves existing `schedule_segment` signature). Seven fixtures land in `rust/temporal/tests/multi_segment.rs`: G1↔G1 sharp corner, G1↔G5 smooth junction, lookahead, per-segment limits, joining convergence stress (5-pointed star), long realistic chain (10 mixed segments), curvature-spike inter-grid sanity sentinel.

**Deviations from plan listing (worth recording so spec/plan can be amended in lockstep):**

1. **Fixture 6 geometry**: plan's first 5 mm G1 segment was infeasible for `v_end=1000, a=65k` (needs ≥7.7 mm); plan's G2 arcs were geometrically disconnected. Both fixed by tracking actual endpoint position and bumping minimum G1 length to 20 mm. The fixture still tests 6 G1 + 2 G5 + 2 G2 mixed-type chain at adaptive N + 3-thread parallelism — its intent is preserved.

2. **Fixture 7 spike geometry softened** from plan listing's height-5-over-3-mm (`[(0,0,0),(1,5,0),(1.5,5,0),(3,0,0)]`) to height-2-over-5-mm (`[(0,0,0),(2,2,0),(3,2,0),(5,0,0)]`). The original was too sharp for the SOCP/SLP relaxation architecture at any N — empirical probing across N ∈ {10, 30, 100, 200} showed solver `DivergedSlp` with `peak v²·κ ≈ 4000–4700` at grid points, well above the 2500 mm/s² centripetal cap. That failure mode is SLP-architectural (separate-axis fragility on sharp turns, plausibly Step-9 SLP-cuts-tightening territory), not adaptive-N policy — outside the v1-vs-v2 distinction the fixture is designed to gate. The wider geometry stays in the convergence regime (v1 N=10: peak grid v²·κ = 2038, peak resampled = 2135, both < 2500), so the fixture meaningfully tests the inter-grid-vs-grid resampling gap that spec §6.6.5's sentinel intent actually targets. Per-axis Cartesian jerk validation deferred to v2 per spec §6.6.5 v1 deferral note.

3. **Two co-required library fixes shipped with fixture 6** (commit `f85b0d79`): (a) `parallel::fan_out_solves` now syncs `states[i].v_start/v_end` from the actual profile endpoints after each solve (success or failure), so an infeasible-endpoint solve's clamped-down velocity feeds back into the joining sweeps — spec §2.3's "achievable v_end is *not* a cheap closed-form" made operational. Without this, joining oscillated indefinitely on short segments with aggressive limits. (b) `BatchOutput.junctions[i].v_junction` reports the post-joining converged value (`profiles[i].samples.last().v`), not the upfront cap, so spec §6.2's "profile endpoints match `v_junction` within ε_velocity = 1 mm/s" is properly tight. `binding_cap` retains its upfront-cap-source semantics; doc comments on `JunctionInfo` updated to explain the dual semantics (commit `d590abbc`).

4. **Determinism pin in Clarabel `DefaultSettings`** (commit `45637e70`): `direct_solve_method: "qdldl"` + `max_threads: 1` pinned per kalico-verifier round-3 recommendation, making the joining loop's deterministic-early-bail premise explicit against future Clarabel feature-flag drift. With Clarabel 0.11.1 + kalico's default features (no `faer-sparse`), these match runtime auto-selection — pinning makes the contract visible in code rather than implicit in `Cargo.toml`.

5. **Adaptive-tolerance `auto_falls_back_on_fixture_4_class` test** (commit `97c02b14`): plan's proposed geometry actually `DivergedSlp` at both `Fast` and `Tight`, so the test would have passed by coincidence (Auto = Tight = both diverged) rather than by genuinely demonstrating Fast→Tight recovery. Replaced with the `prototype.rs` fixture-4 G5 cubic at N=200 with endpoints at 50% of the centripetal MVC cap — empirically `Fast` returns `DivergedSlp { last_max_ratio: 1.032 }` while `Tight` returns `SolvedSlp { outer_iters: 3 }`, and `Auto` recovers via fallback.

**Open follow-ups (non-blocking; tracked here so they don't get lost):**

- v2 adaptive-N policy (curvature-aware densification per spec §7.7 mitigation #1) — gated by Fixture 7 in the original plan; v1 ships because the softened fixture passes, but the original plan's pathological geometry remains a known limitation of the SOCP/SLP architecture. Reopen once Step 9's SLP-cuts work tightens the relaxation on sharp curves.
- Per-axis Cartesian jerk validation in fixture 7 (deferred to v2 fixture per spec §6.6.5; needs third NURBS derivative + arclength→u inversion exposed from Layer 0).
- Per-segment `(d1, d2)` derivative cache in `SegmentInput` (avoids 8 NURBS allocations per junction; v2 perf optimization, plan §744).
- `JunctionInfo.v_cap_upfront: f64` companion field (preserves the upfront-cap-value diagnostic that was lost when `v_junction` switched to the converged value; non-blocking quality-of-life).
- Asymmetric κ + tangent-break case (G1 ↔ smooth-curve with non-aligned tangents): spec §2.2 dispatch text routes this away from the JD sub-case (`max(κ_left, κ_right) > κ_floor`), which leaves the corner under-constrained when one side is G1 and the other is a smooth curve with small κ. The kalico-aware slicer parallel workstream is expected to emit C¹-aligned junctions, so this case shouldn't appear in real input — but a `debug_assert!` would make the assumption observable in fixture testing.
- ~~**Fixture 6 stalls on one infeasible segment** (`StalledOnInfeasibleSegment { last_dirty_count: 1 }`) under realistic-machine limits (`a=65k, j=5e7, v=1000`).~~ **Resolved 2026-04-28** — see entry below.
- **`assert_junction_continuity_for_all` is tautological after library fix #2**: `BatchOutput.junctions[i].v_junction` is set to `profiles[i].samples.last().v`, so `v_end_left ≈ v_jct ≈ v_start_right` holds by construction. The helper still gates a real regression (if `output.junctions[i].v_junction` ever stops being sourced from the profile endpoints, the helper would catch it), but it no longer independently validates the convergence contract. Add a sibling helper that uses the upfront-cap value if/when `JunctionInfo.v_cap_upfront` lands.

**Evidence:** Plan + 19 commits on this branch (range `083c1fff..aa2b63be`). Spec at `docs/superpowers/specs/2026-04-27-layer-2-multi-segment-design.md`; Pi 5 throughput investigation at `docs/research/pi5-socp-throughput-investigation.md`; JD formula verification at `docs/research/junction-deviation-cornering-formula.md`. Multi-segment integration tests at `rust/temporal/tests/multi_segment.rs` (7 fixtures); adaptive-tolerance regression tests at `rust/temporal/tests/adaptive_tolerance.rs`. All 54 tests pass; clippy clean under `-D warnings --all-targets --release`. Per-task spec compliance + code-quality reviews completed by the subagent-driven-development pipeline (`superpowers:code-reviewer` opus on each task).

---

## 2026-04-28

**Fixture 6 `StalledOnInfeasibleSegment` resolved — Lee 2024 SLP-vs-verifier predicate asymmetry, not a geometric infeasibility.** Investigation on branch `fixture-6-stall-investigation`. Direct evidence: segment 9 (terminal G2 rational quadratic quarter-arc, R=20mm, L≈31.4mm, joining-converged v_start=1000 → v_end=0) returned `MaxIterSlp{last_max_ratio: 1.1301}` at the adaptive-N policy's chosen N=80. Standalone N-sweep proves a singleton fragility — N ∈ {40, 60, 70, 75, 78, 79, 81, 82, 85, 90, 100, 130, 160, 200, 300} all solve (`Solved` / `SolvedInexact` ≤ 2e-7); only N=80 hits `MaxIterSlp`. Instrumented `verify::check(best_result)` on the N=80 iterate: `verify.feasible == true` with `worst_violation = 2.22e-16` (machine epsilon). The SLP `last_max_ratio` measures the path-jerk RELAXATION gap (Lee 2024 conservative tangent-cut on `1/√b`, an under-tightening per Lee 2024 §III), not a direct feasibility residual; the authoritative bar is `verify::check`'s per-axis Cartesian jerk on the assembled trajectory, and it accepts.

**Fix:** one-block addition in `rust/temporal/src/topp/output.rs::map_status` — promote `(SlpOutcome::MaxIters, _) ⟶ SolvedInexact { residual: verify.worst_violation }` when `verify.feasible`, falling through to `MaxIterSlp` only when the verifier rejects. Symmetric with the existing Clarabel-MaxIter→SolvedInexact promotion at `output.rs:89`. Fixture 6's acceptance gate tightened from `joining_sweeps ≤ 3` to `joining_sweeps ≤ 3 && JoiningStatus::Converged`, matching the other multi-segment fixtures. All 54 temporal tests pass; clippy clean under `-D warnings --all-targets --release`.

**Why:** During Step-4.5 brainstorming, fixture 6 was tagged "out of scope; needs Step-9 SLP-tightening or geometry adjustment." Empirical evidence shows neither is needed: the geometry IS feasible and the SLP IS finding a feasible iterate — the public-status mapping was simply throwing away verifier-feasible iterates whenever the SLP outer loop's own (looser, conservative) convergence test timed out. Trajectory-time-optimality preserved (the iterate's profile is the math-optimal one verify::check accepts at machine epsilon); CLAUDE.md print-throughput non-negotiable not engaged. Cheaper than the Codex-recommended adaptive-N retry (Fix B), which would re-solve a profile we already have.

**Verifier sub-claim corrections:** kalico-verifier round adversarially rejected my original "FD-stencil aliasing on rational arcs" mechanism for the N=80 fragility. Correct framing per Lee 2024 §V: SLP cut conditioning at the marginal-feasibility band — the convex tangent-below-the-curve cut on `1/√b` is a global underestimator; at N=80 the SOCP-with-cuts iterate sits in the 5–13% over-tight band that the SLP's `SLP_EPS_FEAS=5e-2` rejects but its no-improvement divergence rule does not yet trigger; at neighboring N either the iterate lands below 5% or far enough that SLP progress is detectable. The verifier's central-FD-on-`a` stencil is one integration order smoother than the SLP's central-FD-on-`b`, so the verifier accepts iterates the SLP rejects on this geometry. Conclusion (geometry feasible at N=80) was correct; mechanism description was wrong.

**Adjacent observations surfaced during the investigation (filed as separate non-blocking follow-ups; not part of this fix):**

1. **Probe `vector_derivative` on rational NURBS misses the quotient-rule.** My diagnostic probe and the production `multi/junction.rs` curvature path both call `nurbs::eval::vector_derivative` followed by `curvature_from_derivs`. For a rational NURBS, `vector_derivative` lowers degree on the polynomial numerator only; it does NOT apply `(N'·W − N·W') / W²`. On the R=20 quarter-arc, the geometric κ is constant 1/R = 0.05/mm, but the probe reads κ_endpoint = 0.025/mm and κ_max = 0.0707/mm at u=0.5 (factors of 0.5× and √2 off the geometric value). Doesn't affect fixture 6's outcome (per-axis V_max=1000 dominates either putative centripetal cap), and doesn't affect any landed test status today, but `multi/junction.rs::compute_junction_velocity` is using the same buggy curvature on rational segments and will produce wrong centripetal caps when `a_centripetal_max` becomes the binding cap on rational geometry. Worth a separate spec-level fix during smooth-shaper / Layer-3 work.

2. **Stale maintainer warning in `rust/temporal/src/topp/constraints.rs:240–251` says "do NOT add per-axis Cartesian jerk rows … in `topp::verify::check`" — but `verify::check` already does (`verify.rs:116–133`, axis-jerk against `lim.j_max[axis]`).** Likely a leftover from the Step-9 verifier-stencil SLP work. No behavioral consequence; reconcile during Step-9 follow-up.

**Evidence:** 2 commits on `fixture-6-stall-investigation` (output.rs Fix A + multi_segment.rs gate tightening). Verifier research artifact at `docs/research/maxiterslp-grid-sensitivity-fixture-6.md` (kalico-verifier 2026-04-28; partial-verdict on the original hypothesis, with the mechanism-correction and the empirical-followup recommendation that this investigation then ran). Cross-check via codex:rescue (independent diagnosis; agreed on relaxation-gap framing, initially rejected Fix A on a static-analysis comparison of `last_max_ratio` against `EPS_FEAS` — empirical instrumentation of `verify::check(best_result)` showed the predicates measure overlapping but not identical physics, settling the disagreement in Fix A's favor).

---

**Build-order Step 5 (MCU framework with stub NURBS evaluator) completed.** Implementation per `docs/superpowers/plans/2026-04-28-layer-4-mcu-framework-stub.md`.

New `runtime/` no_std crate ships per-axis Engine state machine; `kalico-c-api/` (renamed from `nurbs-c-api/`) is the umbrella staticlib with two cbindgen headers (`kalico_nurbs.h`, `kalico_runtime.h`). 40 kHz TIM5 ISR designed for H723 (Octopus Pro); cycle-budget gate ready in Surface C scripts but actual numbers TBD until hardware bring-up. Trace-only output stage; runtime-eval slots for PA (Step 9) and IS (Step 8) are ZST `Noop` impls. Pre-flight: workspace migrated to Rust 2024 edition.

**Deviations from plan listing:**

- **Task 11 fixture timing**: the plan's literal seam-tolerance test used durations (`tc*3/2`, `3*tc`) that produced a ~7 mm seam diff against the asserted 0.05 mm tolerance. Test now uses `tc+1` and `1000*tc` so samples land at u≈1 of seg1 and u≈0.001 of seg2 (~24 µm seam diff). Math independently re-derived during review.
- **Task 11 plan-internal coupling**: tests reference `fixtures::load()` from Task 17a's parser. Pulled forward Task 17a Steps 1–2 into Task 11 so engine_tick.rs would compile. Task 17a's commit reduced to a no-op acknowledgement.
- **Task 13 latent UB**: `*mut KalicoRuntime → &mut RuntimeContext` produces overlapping `&mut` under Rust's strict aliasing model when the ISR preempts foreground. Step 5 accepts as documented; Step 6 hardening will refactor to half-split SPSC.
- **Task 13 spec/code visibility gap**: plan's literal FFI accessed `pub(crate)` `Engine::widen_state` from outside the runtime crate. Resolved by adding narrow `pub fn widen` and `pub fn reinit_widen` methods on `Engine`.
- **Task 15 cbindgen `expand`**: plan specified `parse.expand = ["kalico-c-api"]` which requires nightly Rust (`-Zunpretty=expanded`); workspace pins stable. Pivoted to symmetric `[export.exclude]` lists in both cbindgen configs. Maintenance trade-off documented; verified by Codex as sound.
- **Task 17b boundary-loop fault test**: `MAX_BOUNDARY_ITERS = 8` matches `Q_N = 8`, but heapless effective capacity is N-1 = 7. So the boundary-loop fault is dead defense-in-depth — unreachable from the public API. Test left as a Step-6 TODO listing three remediation paths.
- **Task 22 `%*s` ABI bug**: plan-literal `command_kalico_load_curve` allocates one args slot per `%*s` blob, but Klipper convention uses two (length + ptr). Left plan-literal as-is per implementer instructions; flagged for Phase 8 hardware bring-up rework. **Resolved 2026-04-28** — `command_kalico_load_curve` now consumes 2 slots per blob (length + `command_decode_ptr(args[i])`), derives `n_cp`/`n_knots`/`n_weights` from blob byte-lengths (12 B/cp, 4 B/knot+weight) with self-consistency validation, and the format string drops the redundant `n_cp=`/`n_knots=` integer args. Host-side `tools/test_h723_first_light.py` and `tools/test_h723_trace_dump.py` updated to match.
- **Task 22 `OTG_FS_IRQn`**: plan-literal referenced `OTG_FS_IRQn` which doesn't exist on H723 (only `OTG_HS_IRQn`). Fixed in commit `418e0b80`.
- **Tasks 26–29 hardware deferral**: Surface C bring-up scripts (`tools/test_h723_*.py`) ship now without local hardware validation. Per CLAUDE.md memory ("Trident is a test bench; user switches to a stable branch when actually printing"), these will be exercised the first time the user runs them on flashed H723 hardware; bugs surfaced there will be fixed in a follow-up commit. Each script has been syntax-validated and import-tested host-side.

**Open follow-ups (non-blocking; tracked here so they don't get lost):**

- Loom test coverage expansion (gated to Step 6 when live producer surfaces).
- Layer 3 time-reparameterization (gating Step 7 MVP — see spec §7 open question 7).
- F4x integration test (gating Step 6 multi-MCU bring-up).
- TanhPa slot velocity dependency (Step 9 design time decides TickState shape).
- Klipper full-LTO link CI (Step 7 MVP CI work).
- Cycle-budget actual numbers — pending Surface C bring-up.
- `Engine::Default` is `#[cfg(test)]` only after Task 11 review; Step 6 may want production-context constructor.
- Spec/code mismatch on `MAX_BOUNDARY_ITERS` vs heapless cap-7 — decide Step 6 whether to lower MAX, bump Q_N to 9 (rounds up to 16 in heapless), or add test-only injection hook.
- Status-LED pin (`PA13` in `runtime_tick.c`) — verify against actual BTT Octopus Pro pinmap during Surface C bring-up; the LED toggle is a visual signal only (PASS/FAIL gate is the kalico_status response).
- `test_h723_trace_dump.py` plot output — TODO; bring-up gate is the position-error check, not the plot.

**Evidence:** Plan + ~30 commits on this branch. Spec at `docs/superpowers/specs/2026-04-28-layer-4-mcu-framework-stub-design.md`. Surface-C cycle-budget result template at `docs/research/step5-h723-cycle-budget.md` (TBD by user). Surface C bring-up scripts at `tools/test_h723_first_light.py`, `tools/test_h723_cycle_count.py`, `tools/test_h723_trace_dump.py`, `tools/test_h723_soak.py`; chained via `Makefile.kalico:test-h723`.

---

**Build-order Step 6 (Communication protocol and clock sync) completed.** Implementation per `docs/superpowers/plans/2026-04-28-step6-comm-protocol-and-sim-fixes.md`. Layer 5 — host↔MCU comms over Klipper's existing msgproto + 1-byte versioned blob payloads, credit-based flow control with periodic 10 Hz `kalico_status_v6` backstop, multi-MCU sync via per-MCU `ClockSyncEstimator` + arm/commit handshake, half-split SPSC FFI projection (closes Step-5 latent UB acknowledged in spec §6.8), generation-counter curve handles (u32 = u16 slot + u16 generation; ABA window 65280 allocations at Q_N_MAX=256), force_idle flush handshake (Plan-decision A: force_idle first / ack-wait / stream_open=false last), explicit dedicated clock-sync at arm-time (Plan-decision B: §12.3 normative, §12.4 quality gate including `last_dedicated_sample_age ≤ MAX_RTT_AGE_MS`), and §13.1 trace-overflow → `KALICO_FAULT_TRACE_OVERFLOW` latch.

**Deviations from plan listing:**

- **Phase 0 — load_curve hang**: GDB-attach diagnosis pinpointed the root cause to Renode's `cpuType: cortex-m7` model silently dropping writes to `SCB->CPACR` (CP10/CP11 enable bits), so any FPU instruction in the firmware UsageFaulted into `DefaultHandler`'s infinite loop. Primary fix is one line in `tools/sim/h723_sim.resc` (`cpu FpuEnabled true` after `LoadPlatformDescription`); the planned fixture-loader escape hatch (`kalico_load_fixture_curve` under the `kalico-sim` Cargo feature) shipped as a backup but is not strictly required after the FPU fix. Both are gated on `CONFIG_KALICO_SIM=y` — never to silicon.

- **Phase 0 — sim drain wake**: the foreground drain timer never fires under sim because Klipper's SysTick-driven dispatch cannot tick (DWT->CYCCNT freeze). Forked `timer_read_time()` in `src/generic/armcm_timer.c` to read `kalico_sim_cyccnt` under `CONFIG_KALICO_SIM`, and added a sim-only `kalico_sim_isr_wake_drain()` invoked from the TIM5 ISR every 40 fires (~1 ms cadence) to provide a deterministic drain wake.

- **Phase 0 — Gate A trace-stream substitute**: spec §3.3 wanted a trace-stream forward-progress check, but the Step-5 latent UB had `trace.dequeue` reading 0 even when the engine demonstrably advanced. Substituted a `kalico_sim_diag` foreground command exposing `tick_counter` / `drain_calls` / `cyccnt` / `status`; Gate A asserts `tick_counter` advances + engine reaches DRAINED. The proper trace-stream test moved to Gate B post-Phase-1 half-split.

- **Phase 1 — Engine widen_state ownership**: per Round-3 fix B-R3-4, `widen_state` moved off `Engine` and onto `IsrState`. Engine::tick canonical 6-arg signature is the single source of truth across all later phases (raw_cyccnt, &mut WidenState, &CurvePool, &mut Consumer, &mut Producer, &SharedState). The seqlock publish happens at the top of tick so all later phases observe a consistent half-state.

- **Phase 1 — TRACE_RING_N reshape + RAM**: TRACE_RING_N promoted to a public const in `trace.rs` and resized to 1201 (HOST_STALL + 10 ms × 40 kHz + 1). RAM `.bss` grew from ~15 KB to ~50 KB; sim firmware links cleanly.

- **Phase 1 — Unsafe Sync impl on CurvePool**: explicit placeholder added because Phase 2's `UnsafeCell`-bearing PoolSlots break auto-Sync; landed in Phase 1 to preempt churn.

- **Phase 7 — irq_save / irq_restore LTO survival**: Klipper's `-flto=auto -fwhole-program` DCE'd standalone `irq_save` / `irq_restore` symbols because their bodies are inlined at every Klipper callsite. Worked around with thin C wrappers `kalico_irq_save` / `kalico_irq_restore` in `src/runtime_tick.c` marked `__attribute__((used, externally_visible))`, forwarding to the originals; state.rs and stream.rs swap imports to the wrapper names.

- **Phase 7 — flush() *mut RuntimeContext signature**: takes `*mut RuntimeContext` (not just `&mut FgState`) so it can project to `IsrState.queue_consumer` under disabled-IRQ for the transient queue drain. Documented as the one FFI entry that crosses the half-split boundary by design.

- **Phase 8 — clock_sync regression x-axis**: Round-2 fix B04 — the regression's x-axis is `host_time_secs` since the estimator's epoch (saturating_duration_since), NOT `Instant::elapsed()` at recording time (which would put every sample at ~0 on the x-axis). `mcu_time_at_host` uses the regression anchor (mean_x, mean_y) per Round-2 B11-real so the absolute MCU clock projection lands on the regression line, not on a freq×delta naive line through the origin.

- **Phase 8 — arm_all_mcus deadline budget**: Round-1 ARMING-race fix — the wait-for-ack deadline is `arm_lead_time / 2`, not the full lead. Half stays available for in-flight wire latency.

- **Phase 10.4 — credit on_epoch_change reset**: per spec §4.4, `kalico_stream_flush` bumps `credit_epoch` and the host has to discard in-flight rollback assumptions. CreditCounter explicitly snaps `available` back to `capacity` on epoch change.

- **Phase 10.5 — push_segment_with_timeout customization**: the standard producer signature loses callers that want a custom transport timeout per push. Added `push_segment_with_timeout` as the customization point; `push_segment` is a wrapper.

- **Phase 11 — Klipper msgproto category trap**: `sendf()` registers via `_DECL_ENCODER`, `output()` registers via `_DECL_OUTPUT`. `buildcommands.py` de-dupes by msgid and uses the FIRST registration's category — so mixing a `DECL_CTR("_DECL_OUTPUT kalico_status_v6 …")` predeclaration with `sendf("kalico_status_v6 …")` puts the format only in `ctr_lookup_output` → `ctr_lookup_encoder` returns NULL → `command_sendf(NULL, …)` silently no-ops. Rewrote the three async emits (status_v6, credit_freed, fault) to use `output()` so categories match the predeclared CTRs.

- **Phase 11 — Renode pacing on status_v6**: Renode virtualizes time at 0.05–0.5× wall-clock; the 10 Hz `runtime_status_drain` task fires unreliably under that pacing. Gate A's status_v6 wire check is WARN-only; binary inspection of `run_tasks` proves the encoder is non-NULL and the wiring is correct. Surface C re-validates at full clock rate. Same disposition for Gate B item 5 (status-frame correctness) per the Phase 13 implementer note.

- **Phase 12.2 — MAX_BOUNDARY_ITERS alignment**: aligned to `Q_N - 1 = 7` matching the heapless SPSC effective capacity. The Step-5 plan-changes-log noted the fault path was dead defense-in-depth from the public API; gated the `Engine::inject_iter_count` test helper behind `cfg(any(test, feature = "test-injection"))` so production builds neither carry the field nor expose the setter.

- **Phase 13 — Gate B item 5/7 sim-WARN handling**: per the Phase 13 plan + Phase 11/12 implementer notes, items 5 (status frame) and 7 (trace overflow) under Renode pacing land as sim-WARN, not FAIL. Hardware Surface C re-validates. Gate B `--all` mode manages sim lifecycle internally because `kalico_stream_flush` does NOT clear latched fault state by design (preserves fault history for host inspection per `runtime/src/stream.rs::flush` step 7), so each item runs against a fresh sim invocation.

- **Phase 14 — hardware bring-up dispatch**: per CLAUDE.md `feedback_printer_is_test_bench`, the printer is a test bench and the user runs hardware tests manually. The Phase 14 deliverable is the Makefile.kalico chain (split into `test-h723-step5` + `test-h723-gate-b` + the union `test-h723`) and the documentation in `tools/sim/README.md`. Gate B sub-targets reuse `tools/test_sim_gate_b.py --only <case>` against `/dev/ttyACM0`; the driver is sim-agnostic at the msgproto wire layer. User power-cycles between Gate-B items because the latched-fault invariant is the same on hardware as on sim.

- **Phase 15 — measurement scaffolds vs run**: per the Codex Round 4 fix the subagent ships scaffold (`tools/measure_m1_host_stall.py`, `tools/measure_m3_clock_sync.py`, the `--m2-rounds`/`--m2-stir-protocol` extension to `tools/test_h723_cycle_count.py`, and `docs/research/step6-buffer-budget-measurements.md` with TODO_USER_RUN placeholders) and proceeds to Phase 16; the actual 8h Pi 5 + 1M-tick MCU + 24h dual-MCU soaks are user-run (multi-day runtimes outside autonomous-execution scope).

- **Phase 16 — CI matrix expansion**: the existing `.github/workflows/ci-rust-runtime.yaml` already covers the host-rt build/test (workspace rolls in via `cargo build --workspace` + `cargo test --workspace` since `kalico-host-rt` is a workspace member), the panic-symbol grep on the release MCU build, and the cbindgen-no-drift check. Added a new `rust-loom` job that runs the four loom tests (`loom_seqlock`, `loom_spsc_split`, `loom_force_idle`, `loom_curve_pool_alloc`) under `RUSTFLAGS="--cfg loom" cargo test --release`.

- **Phase 16 — Plan-decision C scope**: Step 6's `kalico-host-rt::host_io` is the deliberately minimal ~150 LOC port of `tools/kalico_host_io.py`'s minimum surface (open serial port, identify_handshake, framed send, sync wait_for_response with timeout). NO NAK retransmit, NO async event-dispatch thread, NO identify-during-reconnect race recovery. Production-grade hardening is Step-7 MVP work; the optional `python-bridge` PyO3 feature is reserved for a future shim. The MsgProtoParser is also a stub: no JSON dictionary parse yet, scaffolded so the Step-7 swap-in keeps call sites stable.

- **Step-6 closure-review follow-up fixes** (post-merge integrated review against HEAD):
  - **`tools/kalico_host_io.py` host-side robustness fixes** (legitimate fixes for documented issues, but do NOT close the Gate A regression on their own — see follow-up bullet):
    - Skip `_parser.parse` on `len(pkt) <= MESSAGE_MIN` (NAK / bare-ack frames have empty payload windows; previously generated spurious "index out of range" / "Extra data at end of message" warnings on every NAK).
    - Catch `AttributeError` in rx-loop alongside `OSError` / `SerialException` — pyserial's `socket://` URL handler tears down `_socket=None` on close, so an in-flight `read()` raises AttributeError, not SerialException.
    - Output-frame fan-out: `OutputFormat.parse` collapses every `output()` emit to `#name="#output"`. Tests want to wait on the *specific* frame name (`kalico_status_v6`, `kalico_credit_freed`, `kalico_fault`). Extract the leading whitespace-separated token from `#msg` and re-publish into a per-name queue, additionally promoting `key=value` tokens to top-level dict entries.
    - Tightened rx-loop seq-handling to only forward-advance `_seq` on NAK packets (was: every-packet clobber). Avoids a race where output frames arriving between `_next_seq()` and MCU dispatch could rewind the counter behind sent values.
  - **Sim Gate B item 5 ordering**: original sequence pushed → terminal → arm, but `runtime/src/stream.rs::terminal` transitions the FSM to `Draining` and `arm()` requires `StreamOpenPriming`, so the ordering deterministically tripped `KALICO_ERR_STREAM_STATE_VIOLATION` (-140). Swapped to push → arm → terminal so the engine quiesces to Drained on its own once the trailing segment retires.
  - **Sim Gate B item 6 budget**: `LARGE_T_START_BASE = 1_000_000_000` cycles (≈ 1.92 s MCU) translates under Renode's 0.05–0.5× virtual-time pacing to ~30 s wall-clock just for the engine to *reach* `t_start` — overshooting the 20 s item-6 budget and the 25 s item-5 budget. Reduced to `52_000_000` cycles (100 ms × 520 MHz) which keeps segments comfortably ahead of `widened_now` at arm time while letting the first retire fire within ~1 s wall-clock.
  - **CI host-test feature flag**: `.github/workflows/ci-rust-runtime.yaml` host-test job ran `cargo test --workspace` without `--features test-injection`, so `runtime/tests/max_boundary_iters.rs` (gated on that feature) silently compiled to an empty binary and the §6.6 ABI-cap regression never ran. Added `--features runtime/test-injection` to the workspace test command.
  - **Sim Gate A status — KNOWN-BROKEN on HEAD; tracked as an open follow-up.** The original closure note ("Sim Gate A passes") and the closure-review fix subagent's claim ("Gate A now passes deterministically") were both over-stated. Empirical (verified on HEAD against a fresh Renode sim): identify, fixture loads, and 10-segment push all succeed; the periodic 10 Hz `kalico_status_v6` emit (commit `ebd8e179a`) appears to interact badly with the sim's USART2 backpressure, starving `kalico_sim_diag_response` decode. Test fails with "diag timeout at 22.2s/26.2s/30.2s — never got a successful diag response." The host_io fixes above are real improvements but did not resolve this. Likely sim-pacing-only (Renode 0.05–0.5× virtual-time pacing magnifies status_v6 traffic past the 4s per-poll budget); production H723 hardware will not see this. **Closure disposition: does not block Step-6 closure** because (a) the runtime/wire/protocol surface is independently test-covered (451+ unit tests + loom + sim stream-lifecycle test passes), and (b) the canonical Gate B verifier per spec §3.3 is the H723 hardware bring-up chain (`make -f Makefile.kalico test-h723`), not the sim. Reopen as a follow-up: instrument `_rx_loop` to count packets-per-second and confirm v6 traffic is the saturator, then either lower the sim periodic emit rate or rate-limit on the host side.

  - **Step-6 adversarial-review follow-up fixes (post-closure-review)** — Codex adversarial pass + Opus validation flagged THREE substantive issues that the prior 4-round spec review + 5-round plan review + per-phase code/spec compliance reviews all MISSED:
    1. **HIGH (ship-blocker): credit-event suppression in `runtime_drain`.** `kalico_runtime_drain_trace` consumed `SEGMENT_END` samples and called `pool.confirm_retired` correctly, but didn't report the consumption back to the C caller. The C handler then queried only the second (`drain_and_reclaim`) leg's saw-segment-end bit — which under steady-state push always reads 0 because the first leg already ate every `SEGMENT_END`. Net effect on real hardware: `kalico_credit_freed` async event suppressed → host credit counter drains → host stops pushing → flow-control deadlock.
       - Fix: added a `*mut u8 out_saw_segment_end` out-param to `kalico_runtime_drain_trace`; C handler ORs both legs' bits when deciding to emit. New regression test in `rust/kalico-c-api/tests/drain_trace_credit.rs` enqueues a `SEGMENT_END` and asserts the bit is set.
    2. **HIGH (ship-blocker): host producer push_segment schema mismatch.** `kalico-host-rt::producer::push_segment` was emitting `kalico_push_segment id=… curve_handle_packed=… t_start_lo=… t_start_hi=… … kin=…` while the firmware's `DECL_COMMAND` declares `curve_handle=…`, `t_start_hi=…` BEFORE `t_start_lo=…`, and `kinematics=…`. Three concrete mismatches (field names + ordering) that the Klipper msgproto encoder would silently fail to encode the moment a real data dictionary loads. The unit tests in `tests/producer_unit.rs` locked in the WRONG schema, which is precisely how this shipped past every prior review.
       - Fix: corrected `producer.rs` format string to mirror the firmware DECL_COMMAND verbatim. Updated `producer_unit.rs` assertions to mirror the canonical schema (and added negative assertions on the legacy field names so a regression can't sneak back in). Added a programmatic schema cross-check at `rust/kalico-host-rt/tests/wire_schema_crosscheck.rs` that parses every `DECL_COMMAND` out of `src/runtime_tick.c`, compares the field-name list against host-side emission for `kalico_push_segment` end-to-end, and locks in the literal format strings for `kalico_stream_arm` and `kalico_clock_sync_request`. The other host commands (stream_arm, clock_sync_request) were verified by hand against the firmware DECL_COMMAND set; both already matched.
    3. **MEDIUM (observability gap): `fault_detail` atomic never written.** `SharedState.fault_detail` was declared, encoded, exposed via `kalico_runtime_fault_detail`, and reported on the wire by both `kalico_fault` and `kalico_status_v6` — but no code path stored into it. Always `0`.
       - Fix: extended `Engine::latch_fault` with an `Option<u32> detail` parameter; the four call sites pass `Some(encode_invalid_curve_handle(slot, observed=0, expected=handle.gen))` for InvalidHandle and `None` for the rest. Added stream-state-violation `fault_detail` writes at all three call sites in `runtime/src/stream.rs` via a new `publish_stream_state_violation_detail` helper. Tests extended in `engine_tick.rs::invalid_curve_handle_latches_fault` and a new assertion in `stream_lifecycle.rs::open_with_different_stream_id_violates_state`. Test count grew from 451 → 460.
    4. **Adjacent (Opus-flagged): `kalico_trace` format-version byte exemption documented.** Phase 3.1 added a 1-byte FORMAT_VERSION_V1 = 0x01 prefix on host→MCU blob payloads (load_curve cps/knots/weights). The MCU→host trace blob is intentionally NOT versioned. Inline doc-comment in `src/runtime_tick.c` `runtime_drain` documents the exemption: `TraceSample` schema is sanity-checked at compile time via `static_assert(sizeof == 32)` plus the cbindgen-no-drift CI check; a wire-protocol version bump would change the data dict (different msgid) anyway, so a per-batch version byte adds 1.5% overhead for no decoder benefit.
    - **Honest acknowledgment:** the prior review process failed to catch protocol-level bugs (#1 and #2) because no test exercised real msgproto encoding against the firmware schema; the `producer_unit.rs` mocks mirrored the wrong implementation, locking in the bug instead of catching it. The Python integration test (`test_sim_stream_lifecycle.py`) used the correct firmware schema all along — but only the python path exercised it. Now the Rust path has a programmatic cross-check too.
    - **Lessons-learned for future Step-N reviews:** include a host↔MCU schema cross-check that programmatically parses `DECL_COMMAND` format strings from the firmware C source and compares against host-side emission strings — both literal-string match for static formats and field-name/ordering match via MockTransport capture for dynamic emissions.

**Open follow-ups (non-blocking; tracked here so they don't get lost):**

- F4x integration (parallel workstream; Step-6 ships H723-only sim verification + scaffold for dual-MCU M3 soak).
- Host/klippy IPC boundary deferred to Step 7 MVP. Production-grade `host_io.rs` (NAK retransmit, async event dispatch, reconnect recovery) is Step 7 work per Plan-decision C.
- M1 / M2 / M3 measurement actuals — user-run, pending. `docs/research/step6-buffer-budget-measurements.md` carries TODO_USER_RUN placeholders for all three; user updates the doc and any divergent default constants in `rust/kalico-host-rt/src/clock_sync.rs` / `rust/runtime/src/spsc.rs` after each soak.
- H723 hardware Gate B sub-targets pending user execution: `make -f Makefile.kalico test-h723-gate-b SERIAL_PORT=/dev/ttyACM0` (with reset between item-6 and item-7 because faults latch).
- MsgProtoParser JSON dictionary parse (stub today; Step-7 MVP swap-in).
- Surface C cycle-budget actuals (Step-5 carryover) — tracked in `docs/research/step5-h723-cycle-budget.md`.
- **Sim firmware RAM headroom**: current sim build (`out/klipper.elf` with `CONFIG_KALICO_SIM=y`) reports `text=56072 data=72 bss=49892` against 128 KB of linker-script RAM (≈38 %). The closure-review note flagged a "100 % RAM" reading; we could not reproduce on HEAD against the generic `armcm_link.ld` constraint, but the data-point is worth tracking — Phase-1's TRACE_RING_N reshape moved bss from ~15 KB to ~50 KB, so future struct growth (notably more trace fields, larger `CurvePool`, or a deeper SPSC) will compound. Re-measure after any of the open follow-ups land bigger structs.
- **Sim Gate A regression** (commit `ebd8e179a` periodic-status emit interacts with USART2 backpressure under Renode pacing). See "Step-6 closure-review follow-up fixes" entry above for the empirical state and the proposed instrumentation path. Hardware Gate A on H723 is the canonical verifier per spec §3.3; sim is a developer convenience that's currently broken for this specific test.

- **Round-2 adversarial-review follow-ups** — Codex round-2 review (post-round-1-fixes) found 2 additional issues that are deferred to Step 7 MVP rather than fixed in Step 6. Both validated by Opus; neither is a ship-blocker.
  - **High (latent today; live once Step-7 MVP parser ships): `arm_all_mcus` request_id correlation gap.** `rust/kalico-host-rt/src/stream.rs:111-124` sends `kalico_clock_sync_request request_id=1` (hardcoded constant) and accepts the next `kalico_clock_sync_response` by message-name only — never validates the echoed `request_id`. `KalicoHostIo::wait_for_response` returns the FIRST matching message in `self.pending`, which is only drained during identify, not between arm attempts. **Currently inert** because the production parse path is the stubbed `MsgProtoParser` that returns `None` for everything (per Plan-decision C — production parser deferred to Step 7); MockTransport in tests echoes correctly. **Becomes live the moment Step-7 MVP wires the real parser.** Fix shape: generate a monotonically increasing per-MCU request_id, include it in the command, reject responses whose echoed request_id doesn't match, drain stale entries; regression test with a queued stale `kalico_clock_sync_response` before the current one. Should land alongside the Step-7 parser work.
  - **Medium (observability): `ArmError::QualityGate` carries no detail about which threshold tripped.** `rust/kalico-host-rt/src/stream.rs` collapses every quality-gate failure (residual / drift / sample-age / missing-dedicated) into a single unit `QualityGate` variant. The `ClockSyncEstimator` already exposes `residual_max_in_window`, `drift_ppm()`, `last_sample_age()`, `last_dedicated_sample_age()` — all the data needed to populate `encode_clock_sync_quality(residual_us, drift_ppm)` and to identify which sub-check tripped. Same class as the Round-1 §9.2 `fault_detail`-encoder-dead issue (commit `29f105e8e`) but on the host-side arm path that fix didn't extend to. Operators debugging arm failures get no hint whether to widen residual budget, drift budget, or chase a stale-sample issue. Pure observability, no correctness risk. Fix shape: replace `ArmError::QualityGate` with `ArmError::QualityGate { residual_us, drift_ppm, age_ms, missing_dedicated }`, populate from the estimator at gate-failure time, optionally call `encode_clock_sync_quality` to render a numeric `fault_detail` for telemetry parity with the MCU-side encoding. Step-7 MVP follow-up alongside operational maturity work.

**Evidence:** Plan + 27 commits on this branch (range `c0ef315e..HEAD`) plus the closure-review follow-up commit + the adversarial-review follow-up commits (3 focused fixes). Spec at `docs/superpowers/specs/2026-04-28-step6-comm-protocol-and-sim-fixes-design.md` (5 review rounds; Round-5 final fixes + convergence sign-off). Rust test surface clean (`cargo test --workspace --features runtime/test-injection`: 460 tests pass — up from 451 with the adversarial-review additions; clippy clean; thumbv7em-none-eabihf check clean; sim firmware builds clean). Sim stream-lifecycle test passes (`tools/test_sim_stream_lifecycle.py`). Sim Gate A is currently broken on HEAD (see closure-review fixes bullet above and Open follow-ups); architectural work is independently validated by the Rust test corpus. H723 hardware Gate A + B chain via `Makefile.kalico::test-h723` pending user execution — that's the canonical verifier per spec §3.3, not the sim. CI: `.github/workflows/ci-rust-runtime.yaml` updated with the new `rust-loom` job and `runtime/test-injection` flag on the workspace test command; existing host / mcu-h7 / mcu-f4 / cbindgen-drift / c-smoke / deny / miri / panic-grep / watchdog-canary jobs preserved.

---

## 2026-04-29

**Build-order Step 7 brainstorm: MVP scope rewritten — smooth-ZV/MZV pre-bake, β-medium shaper-aware TOPP-RA, math-exact time-reparameterization (T-A), per-axis-scalar MCU storage, E-follows-shaped-XY foundation. PA still deferred to Step 9. Step 8 narrowed.** Substantial rework of the Step-7 MVP definition and adjacent CLAUDE.md feature-scope, Layer-3, Layer-4, and critical-path observations. Driven by user direction calls during the Step-7 brainstorm session. CLAUDE.md edits in the same commit as this entry.

**Decisions locked (in user-direction order):**

1. **Smooth shapers ONLY in MVP, no impulse zv/mzv.** User: "we are not doing zv/mzv at this point, only smooth-zv/mzv. ... You can see the implementation in the bleeding-edge v2 branch." Closes a contradiction in the original Step-7 wording ("ZV shaper") vs. CLAUDE.md feature scope ("Only smooth shaper support"). Bleeding-edge-v2's `init_smoother` (single-piece degree-4 polynomial of compact support `[-T_sm/2, T_sm/2]`, T_sm = 0.8025/f for `smooth_zv`, 0.95625/f for `smooth_mzv`) is the canonical kernel definition; already pinned by `rust/nurbs/tests/scripts/generate_klipper_reference.py` and the convolve-against-scipy oracle in `rust/nurbs/src/algebra.rs`.

2. **Shaper-aware TOPP-RA wired in MVP via β-medium outer iteration** (vs. β-light scalar derating or β-full inline-SLP-cuts SOCP). User picked "B" then later "medium is better." Outer loop: solve TOPP-RA → pre-bake smooth shaper → check closed-form post-shape peak `|ẍ_shaped|` per axis (polynomial extremum on the shaped NURBS's analytic derivative — closed-form because smooth-shaper output is piecewise-polynomial NURBS, no L¹ bound needed) → if `peak > a_machine`, scale accel limit by `a_machine/peak` and re-solve → iterate to convergence. Math-optimal trajectory at convergence (modulo TOPP-RA grid discretization). 2–3 outer iterations expected per segment; embarrassingly parallel across the existing temporal::multi 3-thread batch executor. The original CLAUDE.md observation framing this as a refinement was written against an impulse-ZV-runtime MVP and is rewritten in this commit.

3. **Per-axis scalar NURBS storage on the MCU** (vs. vector-NURBS-with-shared-knots refit). User picked "A." Forced by smooth-shaper convolution math: X uses `smooth_zv@180Hz`, Y uses `smooth_zv@120Hz`, kernel breakpoints differ, so the Minkowski-sum output knot vectors differ per axis. Per-axis scalar storage matches what the convolve operation actually produces; vector-NURBS-with-shared-knots would force re-knotting onto the union of both axes' breakpoints, doubling per-axis knot count. Curve-pool refactor required (single-NURBS-per-slot today; per-axis-scalar means each segment consumes ~3-4 slots). Mathematically clean.

4. **Math-exact time-reparameterization (T-A): per-TOPP-RA-grid pieces, no fitting.** User: "Are you sure T-A is too heavy? Would be a nice flex if we can pull it off." Adversarial verification round (kalico-verifier × 3 + codex × 3 in parallel) confirmed: (a) **VERIFIED**: under TOPP-RA's piecewise-linear-b convention, per-piece `s(t) = √b_k·(t−t_k) + (b₁/4)·(t−t_k)²` is *exactly* degree-2 in t, residual << 1 µm by construction; composition with G5-cubic geometry gives N pieces of degree 6 per segment. (b) **REFUTED**: T-C single-polynomial-per-segment hits a Jackson convergence floor at ~100-1000 µm because TOPP-RA's bang-bang switching makes x(t) at most C¹ — single-poly cannot bridge non-C² kinks. (c) **VERIFIED conditional**: T-B adaptive-fit achieves 5 µm with grid-aligned merging but adds a fit step that T-A doesn't need. (d) **Literature**: Pham 2017, toppra Python library, Tajima/Sencer 2017, Beudaert 2012, Lee 2024 — all grid-native multi-piece; none supports T-C. (e) **H723 cycles**: all options fit comfortably (T-A worst-case 624 cycles/sample, ~4.5 % of the 13750-cycle 40 kHz budget). (f) **Real-corpus bandwidth analysis** on `scripts/fitter_prototype/corpus/voron_cube_arc_fitted.gcode` and `voron_cube_straight_line.gcode` (240-layer OrcaSlicer prints): T-A bytes/segment ~10.5 KB vs T-B ~2.1 KB; T-A peak burst ~2.5 MB/s during dense low-speed regions, fits H723 USB-HS comfortably. T-A's 12.5 mm cap (N=25 grid pieces × 0.5 mm/piece) only splits 4-6 % of segments because real slicer output is dominated by sub-mm segments — split factor 1.05–1.08×, not 3–5× as initially estimated. Verified artifacts: `docs/research/layer3-time-polynomial-fit-bounds.md` (T-B verifier), `docs/research/single-polynomial-fit-per-segment-conditioning.md` (T-C verifier).

5. **E-follows-shaped-XY foundation: E is *not* a separately-shaped axis.** User: "can we make it so there is no desync? I mean, can we build the foundation in the way that this is just the way it works? without any hacks?" Re-reading bleeding-edge-v2's `kin_extruder.c` and `extruder_smoother.py` showed the bleeding-edge "extruder smoother" is a synchronization *hack* designed to align PA-on-extruder with IS-shaped XY; it exists because E is treated as a separate axis with its own (smoothed) trajectory. The foundational fix: derive E directly from the actual shaped XY motion at MCU sample time. **`E_actual(t) = ratio_per_xy_mm × ∫₀ᵗ √(Ẋ_shaped(τ)² + Ẏ_shaped(τ)²) dτ`** for COUPLED_TO_XY segments (per-sample integration, ~5 extra FPU cycles). INDEPENDENT segments (retraction, prime, filament-change — E motion with no XY) carry their own un-shaped E NURBS. Per-axis IS dispersion is absorbed automatically because E follows actual toolhead arc-length-traveled, not commanded. Step-9 PA layers cleanly on top: `+ advance(sign(v̇_xy)) × ratio × |v_xy(t)|` — same shaped-velocity source, no second IS-PA synchronization step. Zero desync, by construction. Bleeding-edge-v2's 14-variant `extruder_smoother.py` family disappears.

6. **Per-axis kernel inventory clarified.** X and Y configurable and required; Z configurable and default-off (passthrough — user-correction: prior draft incorrectly claimed Z resonance is always >>200 Hz, which is unsubstantiated; real machines vary). E is not a separately-shaped axis (per #5). Other smooth-shaper families (`smooth_ei`, `smooth_2hump_ei`, `smooth_zvd_ei`, `smooth_si`) defer to Step 8.

7. **Step 7 decomposes into 7-A (Layer 3 host) / 7-B (Layer 4 MCU) / 7-C (klippy bridge + production host I/O — closes Step-6 Plan-decision-C deferrals) / 7-D (hardware bring-up).** 7-A is the next sub-project to brainstorm in detail; the spec lands at `docs/superpowers/specs/2026-04-29-step7-a-layer3-mvp-design.md` (next).

**Step 8 narrowed:** loses smooth-shaper application and shaper-aware TOPP-RA (both pulled forward to MVP). Now Step 8 is "corner-blend shape finalization + smooth-shaper-family expansion (smooth_ei / smooth_2hump_ei / smooth_zvd_ei / smooth_si)." Completes Layer 3.

**Step 9 reframed:** PA layers onto the COUPLED_TO_XY E integration — no "base E NURBS" emitted for COUPLED segments because E doesn't have an independent NURBS in MVP. INDEPENDENT-mode E (retraction) gets its own PA path. Asymmetric PA (separate K_accel vs K_decel) dispatches on `sign(v̇_xy)`.

**Why:** the impulse-ZV-runtime framing in the original Step-7 wording was the smallest internally-inconsistent slice with the rest of CLAUDE.md (which already specified "smooth shaper support, pre-baked into NURBS" as the foundation). Pulling smooth-ZV/MZV pre-bake forward to MVP closes the contradiction; doing so unlocks β-medium shaper-aware TOPP-RA and the E-follows-shaped-XY foundation as natural consequences (math-exact time-reparameterization + closed-form post-shape peak both fall out of the algebraic-closure principle once smooth shapers are pre-baked). The result is a Step-7 MVP that's larger in scope than the original "ZV impulse + JD on G1" definition, but the architectural pieces it commits to are the *foundation* of the entire planner — not polish layers that get rewritten later. Per CLAUDE.md non-negotiable-throughput, MVP committing to the math-optimal foundation rather than a thrown-away derating shortcut is the correct trade.

**Evidence:** Step-7 brainstorm transcript this session; user direction calls quoted above; six-agent parallel validation pass (3 × kalico-verifier + 3 × codex:codex-rescue) on the time-discretization options producing the artifacts cited in #4; real-corpus bandwidth analysis at `/tmp/analyze_segment_distribution.py` and `/tmp/analyze_realistic_bandwidth.py` against the 240-layer OrcaSlicer prints in `scripts/fitter_prototype/corpus/`. Spec for 7-A pending — drafting next per the brainstorming workflow.

---

**Build-order Step 7 brainstorm round 2 (same date) — live pipeline narrowed to G5 / G5.1 only with uniform cubic Bézier internal representation; Step 13 expanded from spline-fitter to broader compatibility layer.** Continuation of the same brainstorm session; user direction calls layered onto the round-1 decisions above. CLAUDE.md edits in this commit reflect both rounds; the round-1 entry's "polynomial-refit at Layer 1 entry" plan is *superseded* by this entry — the refit primitive moves to the Step-13 compat-layer scope.

**Decisions locked (round 2):**

1. **Live pipeline accepts G5 / G5.1 only.** G5 → cubic Bézier polynomial NURBS direct; G5.1 → cubic via exact degree-elevation (degree 2 → 3, +1 control point, no fit error). Legacy G0 / G1 / G2 / G3 are *not* handled by the live parser. User direction: "and for now we can make it so we only support g5."

2. **Uniform cubic Bézier (degree-3 polynomial NURBS) representation across all live pipeline layers.** No rational NURBS, no mixed-degree dispatch, no source-gcode-type special-cases. Every segment is a cubic Bézier (single-piece for line/G5; multi-piece for circular arc post-compat-layer); evaluator and storage uniform throughout.

3. **Build-order Step 13 expanded:** was "spline fitter — optional, offline G1-compatibility addon"; now "compatibility layer — offline legacy-G-code → G5-only normalizer." Three reduction paths: G1 → G5 single-piece cubic with collinear control points (degree-elevation, exact); G2 / G3 → G5 multi-piece cubic Bézier via Goldapp 1991 closed-form circular-arc-to-Bézier (~2 pieces per quarter-arc at 0.1 µm L∞ — published per-degree constants, no LSQ); G5.1 → G5 by degree-elevation. Optional G1-sequence spline-fitter for users who want smoother corners (Tajima/Sencer 2016, Beudaert 2012; standard offline-CNC literature applies). Output flows through the live Layer 1+ pipeline.

4. **The polynomial-refit primitive originally proposed for Layer 0 / Layer 1 (round-1 prep work item #1) is dropped from MVP prep scope.** It moves to the Step-13 compat-layer crate, where the Goldapp 1991 closed-form is the right tool — no LSQ, no LWK general-rational machinery needed. The closed-form coefficient lookup + sub-arc subdivision is ~50 LOC (CAGD-published constants), much simpler than the round-1 v2/v3 proposals.

5. **Step-7-A prep work shrinks from 3 items to 2:** composition primitive (`x(s(t))` polynomial-of-polynomial in `nurbs::algebra`) and gcode-side N≤25-grid-piece cap splitter (Layer 1 reduce stage). The G1 / G2 / G3 reduction code paths in `geometry::reduce` retire from the live pipeline; their substance moves to Step-13 / compat-layer scope. Section A of the round-1 brainstorm (refit primitive at Layer 1) is *withdrawn*.

6. **The latent rational-NURBS-curvature bug** (verifier-flagged: `nurbs::eval::vector_derivative` misses the quotient rule on rationals; `multi/junction.rs::compute_junction_velocity` would produce wrong centripetal caps when the rational case binds) **becomes inert forever.** No rational input ever reaches Layer 0/1/2 in the live pipeline. The compat layer lifts everything to polynomial cubic before the live pipeline sees it. Document-and-leave is acceptable; fixing the bug for hygiene is optional.

7. **`Segment::Arc` variant in `geometry::segment` retires.** All segments downstream of Layer 1 are polynomial cubic Bézier NURBS (live pipeline) — `Fitted` (renamed `Cubic`) is the single curve-bearing variant alongside `CornerBlend` and `Junction` slot variants. One curve type through the stack.

8. **`nurbs::algebra::convolve`'s `RationalNotSupported` rejection is dead code in MVP** (no rational input flows live). Can remove the guard or keep as defensive — no perf cost either way.

9. **Test-print workflow:** existing OrcaSlicer test corpus in `scripts/fitter_prototype/corpus/voron_cube_arc_fitted.gcode` and `voron_cube_straight_line.gcode` requires Step-13 normalization before live consumption. Either Step 13 ships before MVP-test-print stage, or the parallel kalico-aware-slicer workstream provides G5 directly. Synthetic G5-only test G-code generated programmatically suffices for Step-7-A/B/C development testing without either dependency.

**Why:** simplification compounding from the round-1 decisions. With smooth shapers pre-baking, T-A math-exact time-reparameterization, per-axis-scalar storage, and E-follows-shaped-XY all locked in, the remaining special-case at Layer 1 was the rational-quadratic-for-G2/G3 representation. Eliminating it (by moving the fit upstream to a separate compat layer) collapses the curve-type dispatch surface to one polynomial type. CLAUDE.md non-negotiable-throughput is unaffected — the trajectory is unchanged; we're just shifting where the polynomial fit happens (Step-13 offline pre-pass instead of Layer-1 live). The compat layer is the right home for that fit because it has access to the full file (better fitting decisions than per-segment live work) and runs once per print rather than per-segment.

**Step-by-step user direction trail:** "is it difficult to have native support for arcs through the entire pipeline? feels like math for them is easier, no?" → led to honest analysis showing convolution math wall forces polynomial-fit somewhere → "okay, A is good. cnc is a very far fetched dream. no need to mention it." → Layer-1 eager refit confirmed → "is there any potential reason a slicer would want to emit a rational NURBS?" → analysis of slicer-side pre-fit vs. kalico-side refit, SL-1 (slicer pre-fits) preferred for FDM → "so if we make a slicer that will take a step file as an input, and produce a true native spline gcode, it might contain ellipses and conics?" → confirmed extensible via dispatch primitive-per-input-type → "it's almost like we could replace g2 and g3 with g5 as well" → led to v4 (eliminate rational from live pipeline) → "and then we can add a compatibility layer that is executed separately before the print, that will convert g1/g2/g3 gcode into g5 only, as a separate later stage. and for now we can make it so we only support g5. Should we represent straight lines as g5 too?" → final round-2 architecture locked.

**Evidence:** brainstorm transcript continued from round-1 entry; literature cross-check via `kalico-researcher` survey of CAGD rational-to-polynomial Bézier conversion algorithms (Sederberg-Kakimoto 1991, Wang-Sederberg-Chen 1997, Floater 1995/2006, Hu-Wang-Jin 2008, Lewanowicz-Woźny-Keller 2012/2015, Goldapp 1991, Vavpetič-Žagar 2015/2017, Kim 2023; OpenCASCADE / Rhino practical implementations). Codex second opinion on Section A (round-1 v2 LWK) flagged knot-vector-sharing concern that propagated into the simplification path. CLAUDE.md edits in same commit; Step-13 build-order entry rewritten.

---

**7-pre spec — 7 rounds of adversarial review (codex + verify-logic loop).** After the 2026-04-29 brainstorms above produced the initial `docs/superpowers/specs/2026-04-29-step7-pre-cubic-pipeline-prep-design.md`, ran 7 rounds of `codex:codex-rescue` reviews with kalico-verifier `verify-logic` on the math claims, applying every codex+verifier-agreed finding between rounds. Convergence reached at round 7 with only 3 LOW (text-polish) findings.

**Findings cumulative across 7 rounds:**

- **Round 1** (initial review): 2 CRITICAL + 4 HIGH + 3 MEDIUM. CRITICAL findings (BezierPiece monomial-not-Bernstein basis; split_piece_at strict-interior asserts vs. param_from_arc_length endpoint clamping) verified via direct codebase reads. HIGH 1 (linear u(s) sub-µm claim) ran through `verify-logic`/kalico-verifier round, returning **REFUTED** with concrete counterexample numbers (S-curve 4.4 µm, R=1mm arc 7.1 µm, near-cusp 100+ µm) — see `docs/research/linear-us-approximation-cubic-bezier-error.md`. The verifier also caught a false claim about Goldapp 1991 (Goldapp optimizes geometric L∞ chord error per Rababah 2016, *not* parameterization-speed-uniformity as the v1 spec claimed). All 9 findings applied: §4.5 fully rewritten with new `fit_x_to_arc_length_piece` primitive (adaptive Chebyshev-node polynomial fit with sample-verified residual bound + adaptive split fallback); §4.3 rewritten for monomial-basis substitution; §5.3 step-1 fast-path moved before arc-length-table build; §5.3 step-6 epsilon filter added; §6.1 CubicSegment E-mode classification rules made explicit; §6.5 G5.1 elevation contradiction fixed; §7.3 test suite split into live + legacy-reference; §8 implementation ordering re-phased.
- **Round 2**: 1 CRITICAL (extrusion ratio used endpoint chord, not XY arc length — corrected to `ΔE / xy_arc_length(xyz)` with new primitive `xy_arc_length`) + 3 HIGH (signed-E classification gap; midpoint-only fit verification too weak; INDEPENDENT-passthrough fast-path bypassed cap for helical-as-Independent) + 2 MEDIUM (stale §7.2 wording, adaptive-split termination policy missing). All applied.
- **Round 3**: 1 HIGH (chord-length passthrough gate could bypass cap for closed/near-closed cubic loops with chord ≈ 0; replaced with control-polygon-length + midpoint-speed AND-gate) + 4 MEDIUM (helical escape underspecified; splitter conservation argument needed clarification; `xy_arc_length` API ambiguous; precision language overstated guarantee) + 2 LOW. All applied.
- **Round 4**: 2 MEDIUM (helical extrusion was silently mis-classified — now hard-rejected with `GeometryError::HelicalExtrusionUnsupported`; `xy_arc_length` test fixture used a wrong identity `√(L_3d² − L_z²)` — corrected to direct numerical projection fixture) + 3 LOW. All applied.
- **Round 5**: 1 MEDIUM (round-4 helical hard-reject left stale references in §5.4, §5.6, §6.1 propagation rules — scrubbed) + 2 LOW. Applied.
- **Round 6**: 1 MEDIUM (one stale "fall through to step 2" sentence in §5.3 missed by round-5 scrub) + 1 LOW. Applied.
- **Round 7**: 3 LOW only (stale degree wording in §4.3, identity-test correctness in §4.7, Independent invariant scoping in §6.1 doc-comment). All applied for polish.

**Why a 7-round loop was needed.** Each round of fixes introduced its own follow-on inconsistencies (stale references in adjacent sections, doc-comments not kept in sync with table-row changes, etc.). Particularly notable: the round-4 helical hard-reject required scrubbing FOUR sections (§5.4, §5.6, §6.1 propagation, §6.1 EMode doc) — round 5 caught the first sweep, round 6 caught the leftover, round 7 confirmed convergence. The `verify-logic`/kalico-verifier round on the round-1 sub-µm-linear-u(s) claim was critical: it wasn't just a text correction, it was a fundamental architectural change (introducing the `fit_x_to_arc_length_piece` primitive that all subsequent rounds refined).

**Convergence criterion:** user instruction was "repeat until they only find minor stuff." Round 7's 3 LOW findings all classified as text-polish nitpicks by codex itself ("mechanical wording fixes with no design implications"). Loop terminated.

**Spec status:** ready for plan-writing transition. The math claims have been adversarially verified; the API surfaces have been pinned against the actual codebase state; the structural refactor scope is concretized; tests are categorized; implementation sequence is phased. 7-pre spec is at `docs/superpowers/specs/2026-04-29-step7-pre-cubic-pipeline-prep-design.md`.

**Evidence:** 7 codex-rescue review transcripts; 1 kalico-verifier `verify-logic` round on the math claim; research artifact at `docs/research/linear-us-approximation-cubic-bezier-error.md`; 36 spec-edit operations distributed across the 7 rounds (commit history reflects).

---

## 2026-04-30

**Build-order Step 7-A (Layer 3 trajectory shaping): completed.** New `rust/trajectory/` crate implements the full Layer 3 host-side pipeline: time-reparameterization (fit x(s) + exact polynomial composition with degree-2 s(t)), C¹-constrained Hermite refit (degree 4, ≤5 µm L∞), per-axis smooth-ZV/smooth-MZV convolution via pad-and-trim, sample-based peak-acceleration check (40 kHz finite differences), β-medium outer iteration on TOPP-RA accel limits, E-follows-XY metadata emission, and independent E trapezoidal scheduling. 62 tests (57 unit + 5 integration).

**Design decisions locked during brainstorm:**

1. **New `trajectory` crate wraps `temporal::plan_batch`** (Option A over extending temporal or trait-based callback). Per Codex second opinion: cleanest dependency graph, no temporal API changes needed, best testability.
2. **β derating is per-segment per axis** (not global). Math-optimal at convergence; `SegmentInput` already supports per-segment `Limits`.
3. **Fit-then-convolve** (not exact-then-convolve or convolve-then-refit). 5 µm fitting error is below motor resolution (~2.5 µm). Produces fewer pieces (4-30 vs N=25), lower output degree (9 vs 11), fewer MCU coefficients.
4. **Pad-and-trim for cross-segment convolution** with variable-width padding (not global convolve or edge-special-case). Verified mathematically exact by kalico-verifier. Variable-width handles short segments (duration < T_sm/2) needing multiple neighbors.
5. **C¹-constrained Hermite fit** (not unconstrained Lagrange). Preserves velocity continuity at TOPP-RA grid joints; convolution raises C¹ to C² or higher.
6. **Sample-based peak-accel check** (not symbolic differentiation + root-finding). Resolves the polynomial numerical instability with narrow shaper kernels (120-180 Hz): Horner evaluation is stable even with ~1e12 coefficients; symbolic differentiation amplifies them catastrophically. Checks what the motor actually experiences at MCU sample rate.
7. **Smooth-shaper kernel = `c·(h²-t²)²`** — degree-4 polynomial on `[-h,h]` with normalization `c=15/(16h⁵)`. Ported from bleeding-edge-v2 `init_smoother` conceptual formula.

**Review cycles:** 3 spec review cycles (Codex + Opus cross-check; 22 findings fixed), 1 plan review cycle (8 findings fixed), 1 post-implementation adversarial review (Codex; 6 CRITICALs reported, 2 confirmed by Opus, 4 rejected), 1 adversarial design review (Codex; beta_warning test contract tightened). Mathematical claims verified by kalico-verifier: s(t) degree-2 exactness, composition degree 3×2=6, convolution degree d_x+d_w+1, pad-and-trim correctness, constant-extension for normalized kernels, phase-2 embarrassingly parallel.

**New nurbs primitives landed:** `BezierPiece::differentiate()`, `BezierPiece::real_roots_in_domain()` (companion-matrix QR), `restrict_to_domain()`, `fit_hermite_c1()` (C¹-constrained adaptive merge fitter).

**Evidence:** Spec at `docs/superpowers/specs/2026-04-30-step7a-layer3-trajectory-shaping-design.md` (3 review rounds). Plan at `docs/superpowers/plans/2026-04-30-step7a-layer3-trajectory-shaping.md` (14 tasks). Implementation: 18 commits on `sota-motion` (range `09d794c86..HEAD`). All 62 trajectory tests + full workspace tests pass; clippy clean.

---

## 2026-05-01

**Build-order Step 7-C-io tail: completed; Phase F restructured.** The original Step 7-C-io spec's §9 Phase F ("Soak + capture corpus") had three iterations of the sim-soak design accumulate structural problems — `socket://` Transport adapter required, capture corpus interim-only (USART2 backend ≠ USB-CDC), wall-clock bounds undefined under Renode's 0.05–0.5x pacing, one-hour leak detection too short to trust, one-way capture cannot satisfy `REQUIRED_SURFACES`. Rather than ship a weakened soak harness, Phase F was split:

- **7-C-io tail** (this work): deterministic test battery + `Clock` trait seam + `tick_once()` reactor entrypoint extraction. A1–A7 covering arithmetic, GC, ordering, and edge-case bugs that hardware testing also cannot reliably catch. `decode_absolute` got a small additive correctness fix (`wrapping_add` so the high-end u64 boundary is test-probeable without debug-mode panics). 16 new tests; full host-rt suite: 113 passing (+ 1 pre-existing parallelism flake in `arm_flow_unit::request_id_is_monotonic_across_arm_attempts`, unaffected by this work).
- **7-D handoff:** Renode sim-soak (now optional bench scaffolding), canonical H723 capture corpus, `python-diff-test` retirement (gated on canonical captures per parent spec §4.13), 24h wall-clock soak, USB-CDC byte-sequence fidelity, real unplug semantics, IWDG real-world pacing, Surface-C cycle actuals.

**Design decisions made during brainstorming:**

1. **Backward-compat constructors.** `ClockSyncEstimator::new` and `Reactor::new` keep existing signatures (defaulting to `RealClock`); new `new_with_clock` siblings accept `Arc<dyn Clock>`. No production call sites or existing tests changed.
2. **Harness inside the crate.** `src/host_io/test_harness.rs` cfg-gated `#[cfg(any(test, feature = "test-harness"))]`. A1/A2/A4 live as `#[cfg(test)] mod` blocks inside `reactor.rs` because they need `pub(crate)` field access; A3/A5/A6/A7 are integration tests in `tests/` because they only need the public API. (A3 ended up inside `reactor.rs` too — `MockTransport` doesn't model `AwaitingResponse` GC state.)
3. **`tick_once()` extraction with `TickOutcome`.** Loop body factored out of `Reactor::run()` into `pub(crate) fn tick_once(&mut self) -> TickOutcome`; literal extraction, no behavior change. `run()` becomes `loop { if matches!(self.tick_once(), TickOutcome::Closed) { break; } }`.

**Review cycles:** 3 codex spec-review rounds during brainstorming (28 findings total fixed, 1 must-fix push-back), 1 codex review on Phase 0 (1 push-back, 0 fixes — pre-existing in-flight changes flagged), 1 codex adversarial review on Phases 1–3 (3 findings: 1 push-back on pre-existing reactor behavior, 2 accepted — A1 boundaries strengthened to actually cross mod-16, A4 strengthened to assert exact byte delta).

---

## 2026-05-01 — CLAUDE.md G5-only rule amended for bridge live-conversion

**What changed:** CLAUDE.md and dependency-graph.md G5-only rule clarified: the planner *reduce stage* (geometry reducer + temporal + trajectory) is the rejection boundary, not the lexer. The `rust/gcode` lexer tokenizes legacy G-code (compat depends on it). Bridge converts live G1/G2/G3 via compat primitives before the reduce stage.

**Why:** Step 7-C-bridge needs to support live G1 from gcode-macros/terminal (user's PRINT_START etc. emit G1). Original rule placed rejection at the lexer, which would have broken compat itself.

**Evidence:** docs/superpowers/specs/2026-05-01-step-7c-bridge-design.md §1.4, §9. Codex review passes 1/2/3 surfaced and confirmed the resolution.

**Evidence:** Tail spec at `docs/superpowers/specs/2026-05-01-step-7c-io-tail-design.md`. Plan at `docs/superpowers/plans/2026-05-01-step-7c-io-tail.md`. Implementation: 14 commits on `sota-motion` (`bd943185d..HEAD` modulo cutover). Parent spec §9 Phase F replaced with back-pointer.
