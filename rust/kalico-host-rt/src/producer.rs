//! Per-axis curve and segment dispatch data types (Phase C-B).
//!
//! `CurveLoadParams` and `SegmentPushParams` have been removed (Task 10
//! push-pieces wiring removal). The old `load_curve` / `push_segment` /
//! `reset_curve_pool` functions (which targeted the now-removed
//! `LoadCurveCubic` / `PushSegment` / `ResetCurvePool` wire messages) were
//! deleted in an earlier task. This module is now empty.
